apiVersion: v1
kind: Service
metadata:
  name: {{ include "Sample-web_Deployment-chart.fullname" . }}
  labels:
    {{- include "Sample-web_Deployment-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Sample-web_Deployment-chart.selectorLabels" . | nindent 4 }}
